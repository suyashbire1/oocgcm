# Based on http://conda.pydata.org/docs/travis.html
language: python
python:
  - "3.5"
  - "3.6"

install:
  - sudo apt-get update
  - pip install numpy netCDF4 xarray dask bottleneck
  - python setup.py install

script:
  - py.test oocgcm
#  - py.test oocgcm --cov=oocgcm --cov-report=term-missing
