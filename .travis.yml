# Based on http://conda.pydata.org/docs/travis.html
language: python
sudo: false # use container based build
notifications:
  email: false

matrix:
  fast_finish: true
  include:
  - python: 2.7
    env: CONDA_ENV=py27-min
  - python: 2.7
    env: CONDA_ENV=py27-dev
  - python: 3.5
    env: CONDA_ENV=py35-dev
  - python: 3.6
    env: CONDA_ENV=py36-dev

before_install:
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
      wget http://repo.continuum.io/miniconda/Miniconda-3.16.0-Linux-x86_64.sh -O miniconda.sh;
    else
      wget http://repo.continuum.io/miniconda/Miniconda3-3.16.0-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda info -a

install:
  - conda create -q -n test_env python=$TRAVIS_PYTHON_VERSION numpy scipy pandas matplotlib netcdf4 xarray numba bottleneck dask
  - source activate test_env
  - python setup.py install

script:
  - py.test oocgcm
#  - py.test oocgcm --cov=oocgcm --cov-report=term-missing
